control.cell.rule.modulo : _controlSpeed {
    base {
        title : "Modulo"
        icon  : "cell.rule.modulo.png"
    }
    shader {
        cellModulo : "cell.rule.modulo.metal" {
            version : (0...1=0) <- elements.version.value
            on      : (0...1=0) <- elements.ruleOn.value
            type    : "compute"
        }
    }
    elements {
        ruleOn.icon   : "cell.rule.modulo.png"
    }
}
