control.cell.rule.pixsort : _controlSpeed {
    base {
        title : "pixsort"
        icon  : "control.cell.rule.pixsort.png"
    }
    shader {
        cellPixsort : "cell.rule.timetunnel.metal" {
            version : (0...1=0) <- elements.version.value
            on      : (0...1=0) <- elements.ruleOn.value
            type    : "compute"
        }
    }
    elements {
        ruleOn.icon   : "control.cell.rule.pixsort.png"
    }
}
