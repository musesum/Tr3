control.cell.rule.average : _controlSpeed {
    base {
        title : "Average"
        icon  : "control.cell.rule.average.png"
    }
    shader {
        cellAverage : "cell.rule.average.metal" {
            version : (0...1=0) <- elements.version.value
            on      : (0...1=0) <- elements.ruleOn.value
            type    : "compute"
        }
    }
    elements {
        ruleOn.icon : "control.cell.rule.average.png"
    }
}
