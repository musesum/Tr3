control.cell.rule.fredkin : _controlSpeed {
    base {
        title : "Fredkin"
        icon  : "control.cell.rule.fredkin.png"
    }
    shader {
        cellFredkin : "cell.rule.fredkin.metal" {
            version : (0...1=0) <- elements.version.value
            on      : (0...1=0) <- elements.ruleOn.value
            type    : "compute"
        }
    }
    elements {
        ruleOn.icon   : "control.cell.rule.fredkin.png"
    }
}
