control.cell.rule.timetunnel : _controlSpeed {
    base {
        title : "Time Tunnel"
        icon  : "control.cell.rule.timeTunnel.png"
    }
    shader {
        cellTimetunnel : "cell.rule.timetunnel.metal" {
            version : (0...1=0) <- elements.version.value
            on      : (0...1=0) <- elements.ruleOn.value
            type    : "compute"
        }
    }
    elements {
        ruleOn.icon : "control.cell.rule.timeTunnel.png"
    }
}
