control.cell.rule.fader : _controlSpeed {

    base {
        title : "Fader"
        icon  : "control.cell.rule.fader.png"
    }
    shader {

        cellFader : "cell.rule.fader.metal" {
            version : (0...1=0) <- elements.version.value
            on      : (0...1=0) <- elements.ruleOn.value
            type    : "compute"
        }
    }
   
    elements {
        ruleOn.icon : "control.cell.rule.fader.png"
    }
}
